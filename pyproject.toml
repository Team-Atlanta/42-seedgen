[project]
name = "42-seedgen"
version = "0.1.0"
description = "OSS-CRS seedgen port"
requires-python = ">=3.10"
dependencies = [
    "oss-crs",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
