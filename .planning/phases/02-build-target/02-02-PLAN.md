---
phase: 02-build-target
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified: []
autonomous: false
requirements: [BLDG-01, BLDG-02, BLDG-03, BLDG-04]

must_haves:
  truths:
    - "All three builder Docker images build successfully"
    - "Docker build does not produce errors for any builder Dockerfile"
  artifacts:
    - path: "oss-crs/dockerfiles/builder-coverage.Dockerfile"
      provides: "Coverage builder Dockerfile"
      min_lines: 10
    - path: "oss-crs/dockerfiles/builder-compile-commands.Dockerfile"
      provides: "Compile commands builder Dockerfile"
      min_lines: 10
    - path: "oss-crs/dockerfiles/builder-callgraph.Dockerfile"
      provides: "Callgraph builder Dockerfile"
      min_lines: 10
  key_links:
    - from: "Dockerfile"
      to: "seedgen-runtime:latest"
      via: "COPY --from"
      pattern: "COPY --from=seedgen-runtime:latest"
    - from: "Dockerfile"
      to: "libcrs"
      via: "COPY --from"
      pattern: "COPY --from=libcrs"
---

<objective>
Verify that all three builder Dockerfiles can be built successfully. This validates the syntax, COPY --from references, and script permissions are correct.

Purpose: Ensure the build-target phase infrastructure is ready before attempting full `oss-crs build-target` integration.

Output: Verified builder images ready for OSS-CRS integration testing.
</objective>

<execution_context>
@/home/andrew/.claude/get-shit-done/workflows/execute-plan.md
@/home/andrew/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-build-target/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify builder Dockerfiles can be parsed and built</name>
  <files></files>
  <action>
Test that each builder Dockerfile is syntactically valid and the build context is correct.

For each builder (coverage, compile-commands, callgraph):
1. Run docker build with a dummy base image to verify Dockerfile syntax and COPY paths
2. Use --target or early exit if needed to avoid needing an actual OSS-Fuzz target

Build command pattern:
```bash
# Create a minimal test base image for syntax checking
docker build -t test-base-image -f - . << 'EOF'
FROM gcr.io/oss-fuzz-base/base-builder
RUN touch /src/compile && chmod +x /src/compile
ENV PATH="/src:$PATH"
EOF

# Test each builder Dockerfile
docker build \
  --build-arg target_base_image=test-base-image \
  -f oss-crs/dockerfiles/builder-coverage.Dockerfile \
  -t test-builder-coverage \
  .
```

If any Dockerfile fails to build, fix the syntax or path issues.

Note: This test validates Dockerfile syntax and COPY paths work. Full end-to-end testing with real OSS-Fuzz targets is Phase 4 validation.
  </action>
  <verify>
    <automated>docker build -t test-base-image -f - /home/andrew/post/42-seedgen << 'EOF'
FROM gcr.io/oss-fuzz-base/base-builder
RUN touch /src/compile && chmod +x /src/compile
ENV PATH="/src:$PATH"
EOF
</automated>
  </verify>
  <done>All three builder Dockerfiles parse and COPY operations succeed with seedgen-runtime:latest</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of builder implementation</name>
  <files></files>
  <action>
Present the completed builder Dockerfiles and scripts for human review. The human will verify that the implementation matches the research recommendations and approve or request changes.
  </action>
  <verify>Human types "approved" or describes issues</verify>
  <done>Human confirms implementation matches research patterns and is ready for Phase 4 validation</done>
  <what-built>Three builder Dockerfiles and scripts for coverage, compile_commands, and callgraph builders. All Dockerfiles reference seedgen-runtime:latest for tools and use libCRS for artifact submission.</what-built>
  <how-to-verify>
1. Review the three Dockerfiles in oss-crs/dockerfiles/:
   - builder-coverage.Dockerfile
   - builder-compile-commands.Dockerfile
   - builder-callgraph.Dockerfile

2. Review the three scripts in oss-crs/bin/:
   - builder-coverage.sh
   - builder-compile-commands.sh
   - builder-callgraph.sh

3. Verify patterns match research recommendations:
   - ARGUS used as CC/CXX
   - Correct environment variables for each builder type
   - libCRS submit-build-output called with correct artifact names

4. If oss-crs CLI is available, optionally run:
   ```bash
   oss-crs build-target --help
   ```
   to confirm the command is recognized (actual execution requires Phase 4 test target).
  </how-to-verify>
  <resume-signal>Type "approved" if Dockerfiles and scripts look correct, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Phase 2 completion checklist:
- [ ] All 3 builder Dockerfiles created and syntactically valid
- [ ] All 3 build scripts created and pass shellcheck
- [ ] Dockerfiles successfully reference seedgen-runtime:latest
- [ ] Scripts set correct ARGUS environment variables
- [ ] Scripts call libCRS submit-build-output with correct artifact names
- [ ] Human verification confirms implementation matches research patterns
</verification>

<success_criteria>
- Docker build succeeds for all three builder Dockerfiles
- Human approves the implementation as matching research recommendations
- Phase ready for Phase 4 validation with real OSS-Fuzz target
</success_criteria>

<output>
After completion, create `.planning/phases/02-build-target/02-02-SUMMARY.md`
</output>
